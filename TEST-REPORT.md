# 润笔桌面端全功能 UI 测试报告

**日期**: 2026-08-30
**测试方式**: 应用真实运行（debug exe）+ UIA 控件树遍历 + 像素级验证 + Win32 API 探针，逐个功能点击验证
**构建**: `desktop/src-tauri/target/debug/runbi-desktop.exe`（1.0.3 发布候选版，含本轮修复）

---

## 一、测试范围与结论总览

| 功能区 | 测试项 | 结果 |
|---|---|---|
| 标题栏 | 润色/回复模式切换 Tab | ✅ 通过 |
| 标题栏 | 生成历史与草稿箱 (Ctrl+H) | ✅ 通过（发现展示问题 #4，已修） |
| 标题栏 | 调整窗口透明度（深色） | ❌ 滑杆拖动不生效 → ✅ 已修复并运行时验证 |
| 标题栏 | 始终置顶 | ✅ 通过（置顶时关闭按钮正确禁用） |
| 标题栏 | 打开设置 / 返回面板 | ✅ 通过 |
| 标题栏 | 模式徽章（回复模式/润色模式） | ✅ 通过 |
| 主面板 | 草稿自动恢复提示（立即恢复/忽略） | ✅ 通过 |
| 主面板 | 回复模式：上下文感知徽章 + draft | ✅ 通过 |
| 主面板 | 意图 chips（数字 1/2/3）点击重生成 | ✅ 通过（0.5s/20 Tokens，草稿随 chip 变化） |
| 主面板 | 行业包意图 chips（律所 5 项）与自定义按钮并存 | ✅ 通过（此前用户报的"自定义按钮不显示"未复发） |
| 主面板 | 差异对比模式 | ✅ 通过 |
| 主面板 | 换个说法（重新生成） | ✅ 通过 |
| 主面板 | 复制结果（剪贴板） | ✅ 通过（内容逐字一致） |
| 主面板 | 自定义指令输入框 + 发送按钮 | ✅ 通过（"改成反问句结尾"生效） |
| 设置 | 模型服务页（Key DPAPI 提示/Endpoint/模型/连接检查） | ✅ 通过 |
| 设置 | 行业模板包下拉（5 项全出、切换生效、注入 sceneHint） | ✅ 通过 |
| 设置 | 人设偏好下拉 | ✅ 通过 |
| 设置 | 自定义回复指令（添加/填写/黄色边框提示/保存） | ✅ 通过（保存后 `customActions` 正确落盘；折叠条点击判定见 #5） |
| 设置 | 皮肤深色/浅色 | ✅ 通过（浅色下水滴按钮消失） |
| 设置 | 桌面体验页（快捷键/自动唤起/自启/截图开关） | ✅ 通过 |
| 设置 | 保存设置/取消 | ✅ 通过 |
| 历史 | 时光机草稿箱 100 条、复制、恢复至主界面 | ✅ 通过 |
| 托盘/热键 | Ctrl+Q 全局唤起 | ✅ 通过（日志 wake shortcut fired） |
| 托盘/热键 | 单实例保护 | ✅ 通过（二次启动让位退出码 0） |
| 透明度 | 浅色主题禁用透明度（水滴按钮隐藏） | ✅ 通过（UIA 确认按钮不存在） |
| 透明度 | 浅色启动强制复位不透明 | ❌ 启动时半透明被照样应用 → ✅ 已修复并验证 |
| 透明度 | 运行时切肤复位透明度 | ❌ 切浅色不清除分层样式 → ✅ 已修复 |
| 数据落盘 | customActions / industryPack / skin / windowOpacity | ✅ 通过 |

**统计**: 28 项验证，4 个真实问题，全部修复并回归。734 前端测试 + 27 Rust 测试全绿（另有 1 项需外部 GlitchTip 的测试按预期忽略），双端 tsc 无错误。

---

## 二、发现的问题与修复

### #1（高）拖动透明度滑杆，窗口实际不透明度不变
- **现象**：滑杆拖到 0.85，窗口 alpha 保持 168；React 状态与持久化值都变了，唯独窗口视觉不变。
- **根因**：缓动插值由 JS `requestAnimationFrame` 驱动。WebView2 在面板失焦/隐藏时会节流 rAF，拖动中的动画循环静默停摆，`invoke` 根本发不出去（启动路径不走 rAF，所以启动时透明度是好的——这正是"启动有效、拖动无效"的原因）。
- **修复**：新增 Rust 命令 `animate_window_opacity`，插值动画整体搬到 Rust 侧（16ms 步进、35% 缓动、动画进行中改目标直接改道），完全不受 WebView 可见性影响；前端滑杆只发目标值。`set_window_opacity` 同时支持 opacity=1 时摘除 `WS_EX_LAYERED`。
- **验证**：RangeValue 设 0.9 → alpha 168→228；设 0.3 → alpha 79。方向、幅度、缓动全部正确。

### #2（高）浅色主题用户启动时，持久化的半透明被照样应用
- **现象**：skin=light + windowOpacity=0.5 启动，窗口以 alpha 128 渲染，浅色界面整体发灰。
- **根因**：`loadConfig` 里判断 `skin === 'light'` 用的是 React state 闭包——`setSkin(savedSkin)` 刚调用还没 re-render，读到的永远是默认值 `'dark'`。
- **修复**：改用本次读出的局部变量 `savedSkin` 判定。
- **验证**：light + 0.5 启动，日志两次 `set_window_opacity(1) -> true`，`WS_EX_LAYERED=False`。

### #3（中）运行时切换皮肤不复位透明度
- **现象**：深色半透明下切到浅色，分层样式残留（浅色窗面 + 旧 alpha 叠加，界面脏灰）；切回深色也不恢复用户设置的透明度。
- **修复**：切肤 `useEffect` 里同步 `set_window_opacity`（浅色→1，深色→用户最近值）。
- **说明**：UIA 模拟点击原生 `<select>` 在本轮环境不可靠，此条以代码修复 + 启动/滑杆两条已验证链路作保证；建议你实测一次"深色半透明 ⇄ 浅色"的来回切换。

### #4（低）历史记录"原文"显示的是整段 prompt
- **现象**：时光机里 reply 条目的原文显示"【历史对话记录】：[我]: …"，而不是用户看到的对话原文。
- **根因**：refine 路径把 `buildScreenReplyRefinePrompt` 的整段 prompt 当作 `originalText` 传给了历史记录。
- **修复**：`handleStartPolish` 新增可选参数 `historyOriginalText`，三处 refine 调用点（chip/自定义指令/话术模板）统一传 `last_message_from_other`（兜底最后一条对方消息）。新记录开始生效，旧记录不回改。

### #5（备注，非 bug）设置页"自定义回复指令"折叠条偶发点不开
- 测试中快速合成点击 `<details>` 有 4 次未展开、慢速点击（按住 ~150ms）均成功；真实鼠标点击未复现失败。UIA 树确认展开后"添加指令/按钮名/黄色边框"功能全部正常。暂不改代码，如你在真机上遇到点不开再反馈。

---

## 三、修复后的交付状态

- 已恢复你的原始配置：**深色皮肤 + 透明度 66%**，应用已重启（托盘常驻，Ctrl+Q 唤起）。
- 透明度滑杆现在可以直接拖动了：拖动过程中 Rust 侧平滑缓动，即使面板中途失焦动画也不会中断。
- 测试期间动过的演示数据（行业包=律所、自定义按钮"转人工"）保留在配置里，可当演示样例；不需要就在设置里删掉。

## 四、遗留备忘（非本轮范围）

- 遥测 DSN 设置仍在"人设与高级"页平铺，建议后续折叠进高级区。
- 历史记录的旧条目（修复前生成的）原文仍是 prompt，仅新记录生效。
- 多显示器坐标场景需副屏实测。
